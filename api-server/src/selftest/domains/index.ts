/**
 * Dominios de selftest — punto de entrada para partir el monolito gradualmente.
 * El runner principal (lucy-flow-selftest.ts) importa y registra estos módulos.
 */
export { registerCatalogCleanupTests } from "./catalogCleanup.js";
