/** Versión del servidor API (panel, health). */
export const LUCY_SERVER_VERSION = "3.3";

/** Versión del prompt maestro de Lucy (V10.10: A16166 — todos los modelos de silla capturados + precio PDF anclado). */
export const LUCY_PROMPT_VERSION = "V10.10";
