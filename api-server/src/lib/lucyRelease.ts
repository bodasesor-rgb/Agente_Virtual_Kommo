/** Versión del servidor API (panel, health). */
export const LUCY_SERVER_VERSION = "3.3";

/** Versión del prompt maestro de Lucy (V10.04: A16075 — embudo proveedor + Sheets + recovery cliente). */
export const LUCY_PROMPT_VERSION = "V10.04";
