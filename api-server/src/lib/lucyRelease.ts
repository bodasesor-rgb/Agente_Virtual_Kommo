/** Versión del servidor API (panel, health). */
export const LUCY_SERVER_VERSION = "3.3";

/** Versión del prompt maestro de Lucy (V10.15: A16244b — regla GLOBAL: ninguna rama mata el chat; siempre `?`). */
export const LUCY_PROMPT_VERSION = "V10.15";
