{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "api-server",
    "lib/db",
    "lib/api-zod"
  ],
  "scripts": {
    "build": "npm run build -w @workspace/api-server",
    "start": "node api-server/dist/index.mjs",
    "sync-deploy": "npm run build && cp api-server/dist/*.mjs api-server/dist/postgres.* deploy/"
  },
  "engines": {
    "node": ">=20"
  }
}
